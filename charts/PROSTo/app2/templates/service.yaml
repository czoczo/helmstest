apiVersion: v1
kind: Service
metadata:
  name: {{ include "jakas_apka.fullname" . }}
  labels:
    {{- include "jakas_apka.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jakas_apka.selectorLabels" . | nindent 4 }}
