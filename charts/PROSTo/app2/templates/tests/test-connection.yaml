apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jakas_apka.fullname" . }}-test-connection"
  labels:
    {{- include "jakas_apka.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jakas_apka.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
